<UserControl x:Class="AeroDynasty.Views.AirportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AeroDynasty.Views"
             xmlns:Page="clr-namespace:AeroDynasty.WPF.Views.Page;assembly=AeroDynasty.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Page:Wrapper Grid.Column="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Airport List" Grid.Row="0" FontFamily="Montserrat" Foreground="{StaticResource LightAccentBrush}"/>
                <Separator Grid.Row="1"/>
                <ListView Background="Transparent" Grid.Row="2" BorderThickness="0" ItemsSource="{Binding Airports}" DisplayMemberPath="Name" Padding="0" Margin="0" FontFamily="Montserrat" Foreground="{StaticResource LightAccentBrush}" />
            </Grid>
        </Page:Wrapper>
        <Page:Wrapper Grid.Column="1">
            <StackPanel>
                <TextBlock Text="Airport List" FontFamily="Montserrat" Foreground="{StaticResource LightAccentBrush}"/>
                <Separator/>
                <ListView Background="Transparent" BorderThickness="0" ItemsSource="{Binding Airports}" DisplayMemberPath="Name" FontFamily="Montserrat" Foreground="{StaticResource LightAccentBrush}" />
            </StackPanel>
        </Page:Wrapper>
    </Grid>
</UserControl>
